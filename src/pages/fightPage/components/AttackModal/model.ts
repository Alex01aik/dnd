import { diceService } from "../../../../shared/services/diceService"

export class AttackModalModel {
    // attack(weaponValue: number, bonus?: number){
    //     const hit = diceService.throwDice(20, bonus);
    //     if(hit){
    //         console.log('hit');
    //         const damage = diceService.throwDice(weaponValue, bonus);
    //     }
    // }
}

export const attackModalModel = new AttackModalModel()